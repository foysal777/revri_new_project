
services:
  web:
    build: .
    ports:
      - "3434:8000"
    env_file:
      - .env
    environment:
      - REDIS_URL=env.REDIS_URL
    depends_on:
      - redis


  redis:
    image: redis:7-alpine
    ports:
      - "6378:6379"

